#cloud-config
ssh_authorized_keys:
%{ for _ in ssh_authorized_keys }
  - ${_}
%{ endfor }

write-kubeconfig-mode: "0644"

users:
  - default
  - name: rke2
    homedir: /var/lib/rancher/rke2
    system: true
